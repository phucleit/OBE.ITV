// import { IsContractsAlreadyExist } from '../schemas/contracts.validate';
// import { ObjectId } from 'mongoose';

export class CreateContractDto {
  // @IsContractsAlreadyExist({
  //   message: '$value already exists. Choose another name.'
  // })
  // code: String;

  // customer: ObjectId;
  // domain: ObjectId[];
  // hosting: ObjectId;
  // ssl: ObjectId;
  // email: ObjectId;
  // google: ObjectId;
  // facebook: ObjectId;
  // content: ObjectId;
  // maintenance: ObjectId;
  // price: Number;
  // startedAt: Date;
}
